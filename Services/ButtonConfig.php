<?php

declare(strict_types=1);

/**
 * @deprecated Use AiButton instead.
 * Kept as a backward-compatibility alias so any existing external code
 * that references ButtonConfig still works.
 */
class ButtonConfig extends AiButton {}
